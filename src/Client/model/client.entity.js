export default class Client {
    constructor(client) {
        this.id = client.id;
        this.name = client.name;
        this.email = client.email;
        this.phone = client.phone;
        // Añade más propiedades según las necesidades de tu aplicación
    }
}